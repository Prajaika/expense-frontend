name: Continuous Integration
on: [push]
jobs:
  call-continuous-integration-reusable:
    uses: Prajaika/github-reusable-workflows/.github/workflows/ci.yml@main
 with:
      project_name: expense-frontend
      npm_install: true
    secrets: inherit

